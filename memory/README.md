# Local Long-Term Memory

This folder stores the assistant's long-term memory database when you run the program.

The database file is:

```text
memory.sqlite
```

It is ignored by Git, so your private chat history is not uploaded.

